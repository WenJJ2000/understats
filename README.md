# django_decision_tree

Create a .env file with :
DB_NAME = the address to your db.sqlite 3 file in the same directory as questions
BASE_URL = base url of ur host server "http://127.0.0.1:8000/"

install required files using pip install -r req.txt
